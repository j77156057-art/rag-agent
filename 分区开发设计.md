# DocMind 分区开发（Region-based Development）设计文档

> 状态：**2.0 已落地（2026-09-09）**。在 1.0 的「物理子目录 + 每分区独立 git」之上，新增三层能力：
> **① 分区即配置（regions.json 声明式，可任意扩展）；② 契约中心 + 依赖方向（防循环耦合）；
> ③ 工具层（分区内受控读写 / 校验 / 跨区安全搬移 / 每区提交 / 跨区变更集整体回滚）**。
> 本文档即交付物之一。照「1 目标 → 2 架构 → 3 分区 → 4 Git → 5 工具层 → 6 UI → 7 已落地 → 8 待做」阅读。

---

## 1. 要解决什么问题

游戏等分工开发里，AI（或人）改代码时最容易犯的错是**把不同职责的东西堆到一起**：数值表、美术引用、异常日志、角色行为逻辑全混在几个文件里，越改越乱；一旦改坏，回滚要连坐整库。

本设计给 DocMind 增加 **「分区开发」工作流**：

- 一个**开发按钮** → 自动建目录 + 写分区文档 + 写分区契约 + 每区 git init；
- 代码按分工落在**独立子目录**（素材区 / 数值区 / bug 区 / 角色行为区 / 关卡区 / UI 区 / 音频区 / 网络存档区）；
- 每个分区是**独立 git 仓库**，可单区提交 / 回滚；一次功能跨多区用**变更集**整体回滚；
- Agent 的写操作被**约束在对应分区内**（越区写被拦截），从根上防代码堆叠 / 混乱；
- **依赖方向单向 + 契约校验**：把「防堆叠」从文件层升到语义层，禁止循环耦合。

---

## 2. 架构总览

```
开发模式按钮（🛠 开发模式）
        │  POST /api/init_regions
        ▼
init_regions(root)
        ├─ 读取分区配置（regions.json，缺省用 DEFAULT_REGIONS 的 8 区）
        ├─ 建各分区子目录（git init -q + 设本地提交身份）
        ├─ 写导出接口桩（如 assets/manifest.json、values/balance.schema.json）
        ├─ 写 regions.json                 （配置驱动，用户可改）
        ├─ 写 DEV_INDEX.md                （分区总索引，给人/Agent 快速定位）
        └─ 写 DOCMIND_RULES.md            （分区契约 → 注入 Agent 系统提示，优先级高于通用指引）

代码库根目录/
├─ regions.json                  ← 分区声明（key/dir/depends_on/exports/verify）
├─ assets/       素材区      git ✓   depends_on:[]            exports:[manifest.json]
├─ values/       数值区      git ✓   depends_on:[]            exports:[balance.schema.json]
├─ bugs/         bug 区      git ✓   depends_on:[]
├─ behaviors/    角色行为区  git ✓   depends_on:[values,assets]
├─ levels/       关卡·场景区 git ✓   depends_on:[assets,values]
├─ ui/           UI·HUD 区   git ✓   depends_on:[values]
├─ audio/        音频区      git ✓   depends_on:[assets]
├─ net/          网络·存档区 git ✓   depends_on:[values]
├─ DEV_INDEX.md
└─ DOCMIND_RULES.md           ← /api/ingest_code 会把它注入 Agent 系统提示
```

**为什么复用现有机制**：DocMind 已有 `code_root` 沙箱、`apply_edit`/`create_file`/`run_command`（先读后写 + 体积上限 + `.py` 语法校验 + 人工确认）、以及 `DOCMIND_RULES.md` → Agent 系统提示的注入通道。分区开发只是在这之上加一层「目录 + 契约 + git」，不重写内核。

---

## 3. 分区定义（配置驱动）

分区在 `regions.py` 的 `DEFAULT_REGIONS` 声明；若 `code_root/regions.json` 存在则以它为准（用户可改）。每区字段：
`key` / `dir` / `name` / `desc` / `access`（接入方式） / `depends_on`（依赖的其它分区 key，构成 DAG） / `exports`（对外接口文件） / `verify`（该区校验命令，空=跳过）。

| key | 目录 | 中文名 | depends_on | exports | 接入方式 |
|-----|------|--------|------------|---------|----------|
| `assets` | `assets/` | 素材区 | — | `manifest.json` | 其它分区经接口拉取，不内联源码 |
| `values` | `values/` | 数值区 | — | `balance.schema.json` | 集中配置，避免散落各处 |
| `bugs` | `bugs/` | bug 区 | — | — | 异常归集，按分区归因 |
| `behaviors` | `behaviors/` | 角色行为区 | `values`,`assets` | — | 状态机/行为树，与数值解耦 |
| `levels` | `levels/` | 关卡·场景区 | `assets`,`values` | — | 引用数值与素材，不写逻辑 |
| `ui` | `ui/` | UI·HUD 区 | `values` | — | 引用数值做显示，不内联业务 |
| `audio` | `audio/` | 音频区 | `assets` | — | 引用素材区音频文件 |
| `net` | `net/` | 网络·存档区 | `values` | — | 独立演进，谨慎改（最易出 bug） |

新增/调整分区：改 `DEFAULT_REGIONS`，或在 `regions.json` 覆盖。**依赖方向是单向 DAG**，如 `behaviors→values/assets`，禁止反向，避免循环耦合。

### 3.1 Agent 研判分区（默认 8 区仅作「初始建议」）

真实工程的目录组织千差万别，硬编码 8 区未必贴合。2.0 的设计是：**`DEFAULT_REGIONS` 的 8 区只是初始建议，真正的分区由 Agent 依据代码库实际结构判断后应用**。

机制（`regions.py`）：
- `propose_regions(root)`：扫描代码库**顶层目录名**与**资源类型扩展名**（图像/音频/模型/字体/数据表/代码），对 8 个默认分区各给出 `detected`（是否检出信号）与 `evidence`（命中证据）；核心分区 `assets/values/bugs` 恒建议启用，其余按信号；并识别出**代码库特有的可独立模块**（`custom_suggestions`，即未被默认关键词命中的纯代码模块目录）作为增补候选。
- `_normalize_regions(list)`：校验/补全自定义分区字段（缺 `key/dir` 丢弃、`depends_on/exports` 兼容「列表」或「逗号串」），保证写入 `regions.json` 的结构合法。
- `init_regions(root, regions_list=None)`：新增 `regions_list` 参数——传入自定义清单时，归一化后**覆盖写入 `regions.json` 并初始化**，使分区从「默认建议」变为「Agent 判定方案」。

工具层（`tools.py`，均注册 `TOOLS`）：
- `list_dir(path?)`：限定 `code_root` 的目录勘察，供 Agent 看清淡层模块/资源目录。
- `dev_propose_regions()`：调用 `propose_regions`，返回带证据的建议清单（中文结论）。
- `dev_apply_regions(regions)`：把最终分区清单（JSON 数组或 `{"regions":[...]}`）交给 `init_regions` 落地，并重注入 `DOCMIND_RULES.md` 使约束立即生效。
- `dev_add_region(key, dir, name?)`：向现有配置追加/覆盖单分区并立即初始化，无需重传整套方案。

典型工作流：**`list_dir` 勘察 → `dev_propose_regions` 拿基线（看 `detected`/`included` 决定增删）→ `dev_apply_regions` 落地**（或仅 `dev_add_region` 补一个分区）。应用后 Agent 写操作即被约束到新分区内，后续走 §5 的受控流程。

---

## 4. Git 模型（每分区独立仓库 + 变更集）

- `init_regions` 对每个分区目录执行 `git init -q`，并立即设置**本地提交身份**（`user.email=docmind@local` / `user.name=DocMind`），避免无全局 git 身份的机器提交失败。各仓库彼此独立，`git commit` / `git revert` 只影响该分区。
- 若**父目录已是 git 仓库**，`init_regions` 会把各分区目录追加进父 `.gitignore`（幂等、只追加），避免双重跟踪。
- **跨区变更集**：`commit_all` 把一次功能改动在各分区分别提交，并把 `{id, message, commits}` 记进 `dev_changesets.jsonl`；`rollback_changeset` 对每个分区 `git revert` 其记录的 commit，实现**整体回滚**。
- `list_regions` 返回每个分区的 `git` / `branch` / `dirty`（未提交改动）。

> 取舍：选「独立仓库」而非「单仓库分支 / tag」，是因为要的是「出错后只回滚该分区」，独立仓库语义最干净；跨区功能再用变更集把多区绑定回滚，比单仓库更可控。

---

## 5. 工具层（dev_*）— 2.0 全部实现

所有 dev 工具均以 `code_root` 沙箱为基，再叠加**分区前缀校验**（目标路径必须以对应分区目录开头，否则拒绝），形成双层沙箱。写操作复用 `apply_edit`/`create_file` 的全部护栏（先读后写、`.py` 语法校验、200KB 上限、人工确认）。

| 工具 | 作用 | 关键护栏 |
|------|------|----------|
| `dev_list_regions()` | 列出分区 key/依赖/导出/脏状态 | 供 Agent 选对分区 |
| `dev_region_read(region, path)` | 读某分区内文件 | 越区读被拒 |
| `dev_region_edit(region, path, old?, new)` | 受控改/建某分区文件 | 复用 apply_edit/create_file + 分区前缀校验 |
| `dev_region_verify(region)` | 跑该区 `verify` 命令或导出接口自检 | verify 建议为脚本（非 `python -c`，会被安全策略拦截） |
| `dev_refactor(src, src_path, dst, dst_path)` | 跨区安全搬移 | 校验依赖方向（禁反向倒置分层）；目标不覆盖；源区 git 移除源文件 |
| `dev_commit(region, message)` | 单区提交 | `git -C <region_dir>` 作用域隔离 |
| `dev_verify_contracts()` | 全量契约校验 | 依赖无环 + 被依赖区导出文件存在 + 依赖目标存在 |
| `dev_rebuild_index()` | 重算 `DEV_INDEX.md` | 分区/依赖变动后调用 |
| `dev_commit_all(message)` | 跨区提交 + 生成变更集 | 记 `dev_changesets.jsonl` |
| `dev_list_changesets()` | 列出变更集 | 含 id/message/涉及分区 |
| `dev_rollback_changeset(changeset)` | 整体回滚某变更集 | 各分区 `git revert` 其 commit |
| `list_dir(path?)` | 勘察代码库目录结构（限定 `code_root`） | 研判分区前的「侦察」工具 |
| `dev_propose_regions()` | 据真实代码库研判分区方案（带 `detected`/`included` 证据） | 默认 8 区仅作初始建议 |
| `dev_apply_regions(regions)` | 把最终分区清单（JSON 数组/`{"regions":[...]}`）落地 | 覆盖写 `regions.json` + 初始化 + 重注入契约 |
| `dev_add_region(key, dir, name?)` | 追加/覆盖单分区并立即初始化 | 按需增补，无需重传整套方案 |

`init_regions` 已注册为 Agent 工具与端点；dev_* 全部在 `tools.TOOLS` 注册，并写入 `agent.SYSTEM_PROMPT` 的工具清单与选择指引。`list_dir`/`dev_propose_regions`/`dev_apply_regions`/`dev_add_region` 构成「Agent 研判分区」闭环。

---

## 6. UI 工作区面板（分区开发工作台）

左侧栏「🛠 分区开发」卡片（显示已初始化分区数）；点「🛠 开发模式」打开**分区开发工作台**弹窗（620px 宽）：

- 顶部操作条：`初始化分区` / `刷新状态` / `重算索引` / `全部提交`；
- **智能研判分区**：新增区块，点 `研判分区方案` → 调 `POST /api/propose_regions` → 渲染建议分区卡片（名称/目录/已检出·建议启用徽章/依赖/证据）；`应用推荐方案` 把「建议启用」的默认分区 + 代码库特有模块一并 `POST /api/apply_regions` 落地；另提供 `新增分区`（key/dir/中文名）迷你表单 → `POST /api/add_region` 按需增补；
- **依赖图（SVG）**：按依赖层级自动布局，节点为各分区，箭头表示 `depends_on` 方向，一眼看清单向 DAG；
- **分区状态**：每区卡片显示 名称/目录/说明/依赖/导出/ git 分支/ 是否脏，并提供 `校验`、`提交` 按钮；
- **变更集**：列出 `dev_changesets.jsonl` 中的变更集，每个带 `回滚` 按钮（二次确认后整体 revert）。

后端对应新增端点：`GET /api/regions`、`GET /api/verify_contracts`、`POST /api/rebuild_index`、`POST /api/region_verify`、`POST /api/commit_all`、`GET /api/changesets`、`POST /api/rollback_changeset`、`POST /api/dev_commit`；以及研判相关 `POST /api/propose_regions`、`POST /api/apply_regions`、`POST /api/add_region`。

---

## 7. 本次已落地（文件改动）

| 文件 | 改动 |
|------|------|
| `regions.py` | **重写为 2.0**：`DEFAULT_REGIONS`（8 区，含 `depends_on/exports/verify`）、`REGIONS` 兼容别名；`load_region_config`/`get_region_map`（支持 regions.json）；`init_regions`（建目录 + git init + 本地身份 + 导出桩 + README + regions.json + DEV_INDEX + DOCMIND_RULES），新增 `regions_list` 参数支持自定义方案覆盖写；`verify_contracts`（依赖无环/导出存在/目标存在，DFS 环检测）；`commit_region`/`commit_all`（变更集）/ `list_changesets`/`rollback_changeset`；`rebuild_dev_index`/`list_regions`；**新增 Agent 研判分区**：`propose_regions`（扫描顶层目录与资源类型给带证据的建议 + `custom_suggestions`）、`_normalize_regions`（校验/补全自定义方案）、`_analyze_root`/`_detect_signals`/`_custom_suggestions` 辅助。 |
| `api.py` | 新增端点：`GET /api/regions`、`GET /api/verify_contracts`、`POST /api/rebuild_index`、`POST /api/region_verify`、`POST /api/commit_all`、`GET /api/changesets`、`POST /api/rollback_changeset`、`POST /api/dev_commit`；**研判相关**：`POST /api/propose_regions`、`POST /api/apply_regions`、`POST /api/add_region`；扩展 `regions` 导入（`load_region_config`/`propose_regions`）。 |
| `tools.py` | 新增 `_parse_keyed`/`_require_regions`/`_resolve_region_path`/`_emit_edit_arg`/`_run_region_cmd` 辅助；实现全部 `dev_*` 工具并注册 `TOOLS`；**新增 Agent 研判分区工具**：`list_dir`（目录勘察）、`dev_propose_regions`、`dev_apply_regions`、`dev_add_region`。 |
| `agent.py` | `SYSTEM_PROMPT` 工具清单新增 `init_regions` + 全部 `dev_*`，并在选择指引中增加「分区开发」工作流，以及「Agent 研判分区」工作流（默认分区仅作初始建议，由 Agent 据真实代码库判断后用 `dev_apply_regions`/`dev_add_region` 落地）。 |
| `web/index.html` | 「分区开发工作台」弹窗升级：依赖图 SVG + 分区卡片（依赖/导出/脏状态 + 校验/提交按钮）+ 变更集列表（回滚）；**新增「智能研判分区」区块**（研判/应用推荐方案/新增分区）；新增 CSS 与 JS（drawDepGraph/verifyRegion/commitRegion/commitAll/loadChangesets/rollbackChangeset/rebuildIndex，以及 propose/apply/add 相关）。 |

**验证**：已用临时目录离线跑通 `init_regions`（8 区目录 + 各自 `.git` + 本地身份 + 导出桩 + DEV_INDEX/DOCMIND_RULES 正确）；`dev_region_read/edit` 跨区读/写被拦截；`dev_region_verify` 导出自检通过；`dev_commit`/`dev_commit_all` 提交并生成变更集；`dev_refactor` 依赖方向校验生效（values→behaviors 被拦、bugs→behaviors 允许）；`dev_rollback_changeset` 整体 revert 成功；`api`/`tools`/`agent` 语法与导入无误；前端 JS 经 `node --check` 通过。**Agent 研判分区**：`propose_regions` 对构造代码库（含 assets 媒体目录 / values 数据表 / 纯代码模块）能正确给出 `detected`/`evidence` 与 `custom_suggestions`；`dev_apply_regions` 经 `init_regions(root, regions_list)` 覆盖写 `regions.json` 并初始化；`dev_add_region` 能向现有配置追加分区；`list_dir` 沙箱内目录勘察正常。

---

## 8. 待做 / 下一步（按优先级）

1. **素材接口 `dev_asset_get` ✅**：其它分区通过素材区接口取得安全路径和元数据，不内联美术源码；支持 `manifest.json` 的资源 ID 映射。
2. **bug 归集 `dev_capture_bug` ✅**：把异常、堆栈、来源分区和复现信息写入 `bugs/BUG-*.json`；桌面启动异常和 Agent 对话异常会自动尝试归档。
3. **verify 命令落地**：为各分区在 `regions.json` 填真实 `verify`（如 `pytest`、`npm run build`），让 `dev_region_verify` 真正跑测试/构建而非仅导出自检。
4. **面板补全**：分区内文件树浏览、单区 `git log` 展示、bug 列表展示。
5. **真实工程验证**：拿一个游戏工程跑端到端，迭代分区划分与契约文案。`regions.json` 可由用户按工程实际调整。（历史 T5 已于 2026-09-10 完成首轮；后续待办见 `HANDOFF.md` §5）

---

## 9. 风险与审查点

- **嵌套 git 仓库**：分区是独立仓库，父仓库已被加进 `.gitignore`；`ingest.py` 的 `_SKIP_DIRS` 含 `.git`，索引代码时不会误索 `.git` 内部（已确认）。
- **契约约束力**：`DOCMIND_RULES.md` 是「软约束」（注入系统提示），真正不可越界靠 `dev_region_*` 的分区前缀校验（§5）。2.0 已落地硬拦截。
- **路径安全**：`regions.json` 中的分区目录和导出文件必须是 code_root 内的相对路径；拒绝绝对路径、盘符、`..`、通配符和空路径，避免配置把 Git/写操作带出工程根目录。
- **变更集一致性**：没有改动时不生成空变更集；回滚前检查分区工作区是否干净，任一分区 revert 失败时整体返回失败并逐区报告。
- **依赖方向**：`dev_refactor` 仅禁止「把代码挪进已依赖源的分区」（防倒置分层/循环耦合）；无依赖关系的前后分区间搬移视为解耦重组，允许。改 `regions.json` 的 `depends_on` 后须跑 `dev_verify_contracts` 确认无环。
- **mock provider 不调工具**：端到端演示分区读写需 `ollama`/`qwen`（`mock` 走固定脚本，不会真调写工具）。
- **local embedding 随机**：分区内语义检索无意义，需 `EMBEDDING_PROVIDER=qwen` + Key。
- **git 身份**：`init_regions` 已为各分区设本地身份；若手工 `git init` 分区，需自行 `git config user.*`，否则 commit 会因「Author identity unknown」失败（表现为 dirty 不降）。
