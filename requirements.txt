openai
chromadb
fastapi
uvicorn
pypdf
python-dotenv
httpx
markdown
python-multipart
pywebview
pillow
imageio
imageio-ffmpeg
python-docx
reportlab
openpyxl
python-pptx
# Optional observability backend. Runtime loads it lazily and remains offline
# when DOCMIND_LANGSMITH/LANGCHAIN_TRACING_V2 is not enabled.
langsmith>=0.1
# Standard component interfaces only; the Harness keeps its own Agent loop.
langchain-core>=1.0,<2
# Optional workflow graph backend. The harness falls back to its deterministic
# manager when LangGraph is not installed.
langgraph>=1.0,<2
langgraph-checkpoint-sqlite>=3.0,<4
# Optional production checkpoint backend; SQLite remains the local fallback.
langgraph-checkpoint-postgres>=2.0,<4
psycopg[binary,pool]>=3.2,<4
