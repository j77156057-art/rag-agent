# 游戏项目分区模板 + 项目规则（DocMind 用）

> 目标：给 AI 一个**可检索、可单点修改、可验证**的骨架，从机制上压制「代码堆叠 / 幻觉 / 乱改」。
> 用法：① 按 §1 建目录骨架；② 把 §4 整段另存为 `code_root/DOCMIND_RULES.md`；③ 用 DocMind 索引该目录（网页「📁 索引代码目录」或 `POST /api/ingest_code`）。规则会自动注入 Agent，无需改代码。

---

## 1. 目录骨架（`code_root`，`.py` 换成你的语言同理）

```
game/                          # ← 索引根（code_root）
├── Docs/                      # 策划案/GDD/数值设计 → 走【文档库】search_knowledge（网页上传，暂不支持目录批量）
├── core/                      # 引擎入口 + 游戏循环 + 全局状态（改动最少、最核心）
│   ├── main.py                # 启动、初始化顺序
│   └── events.py              # ★ 全局事件总线：分区之间只靠它通信
├── combat/                    # 伤害计算 / buff / 暴击 / 死亡
│   ├── damage.py              # ★ 伤害公式的唯一权威实现（"伤害计算在哪"就定位到这）
│   └── buff.py
├── player/                    # 移动 / 输入 / 动画状态机
├── enemy/                     # 怪物 AI / 行为 / 刷怪
├── inventory/                 # 物品 / 装备 / 合成
├── ui/                        # HUD / 血条 / 背包界面
├── audio/                     # 音效 / 音乐管理
├── data/                      # 数值表/配置（json/yaml/csv/toml）—— 数据驱动，改数值不碰逻辑
├── assets/                    # 素材引用清单（配合 search_assets）
└── utils/                     # 纯函数工具（无游戏状态，人人可调）
```

## 2. 三条硬规则（比目录名更重要）

1. **依赖方向单向**：`core/`、`utils/` 可被任何人引用；业务分区（combat/player/enemy…）之间**禁止直接 import**，只能通过 `core/events` 发事件解耦。否则改「伤害」会牵连「血条」一起崩。
2. **逻辑与数据分离**：攻击力/血量/速度一律进 `data/`，代码里不硬编码。改数值 = 改配置，AI 不动逻辑。
3. **每类问题有唯一归属分区**（见 §3 路由表）。

## 3. 问题 → 分区路由表

| 你问的话 | 应命中的分区 |
|---|---|
| 伤害计算/暴击/眩晕/buff 在哪 | `combat/` |
| 角色移动/跳跃/输入 | `player/` |
| 怪物 AI/行为 | `enemy/` |
| 背包/物品/装备 | `inventory/` |
| 血条/HUD/背包界面 | `ui/` |
| 音效/背景音乐 | `audio/` |
| 攻击力/血量数值是多少 | `data/` |

## 4. 项目规则文件 —— 下面整段另存为 `DOCMIND_RULES.md`

```markdown
# 项目规则（AI 修改代码前必须逐条遵守）

你是本项目的协作者。调用任何【写】工具（apply_edit / create_file / run_command）前先读本节；违反任一条即视为失败。

## 0. 分区归属（最高优先级）
本工程按分区组织，每个改动只属于一个分区：
- 伤害公式/暴击/buff/死亡 → combat/
- 移动/输入/动画状态机 → player/
- 怪物 AI/行为 → enemy/
- 背包/物品/装备 → inventory/
- HUD/血条/界面 → ui/
- 音效/音乐 → audio/
- 数值/配置 → data/（只改数据，不写逻辑）
- 引擎入口/事件总线 → core/
- 纯函数工具 → utils/

先判断改动属于哪个分区，只在该分区内 read_file / apply_edit / create_file。
跨分区引用必须经 core/events（事件总线），禁止业务分区之间直接 import。

## 1. 先检索，后动手（防堆叠）
新建任何函数/类/文件前，必须先 search_code 或 grep 检索同义符号，确认项目里没有现成实现。
检索到已有实现 → 复用它或 apply_edit 改它，绝不另写一份重复代码。
回答"X 在哪实现"时，必须给出 grep/read_file 返回的真实文件与行号，禁止凭记忆编造。

## 2. 先读，后写（防幻觉/乱改）
任何 apply_edit 之前，必须 read_file 看过目标文件当前内容。
能用 old_text 精确局部替换，就不要整文件重写。

## 3. 单点修改
一次只改一个文件、一个关注点；不顺手重构无关代码。

## 4. 数据与逻辑分离
数值（攻击力/血量/速度）写进 data/ 的配置，不硬编码进代码。

## 5. 改完即验证
改动涉及可运行代码时，用 run_command 跑对应测试/构建；失败就按报错修复，而不是猜测。

## 6. 只引用事实
引用代码片段/符号/行号时，只引用工具返回的真实内容，禁止编造。
```

## 5. 怎么接进 DocMind（本步已实现，2026-09-09）

1. 把上面的目录骨架建好，并在 `code_root` 根放一份 `DOCMIND_RULES.md`。
2. 网页「📁 索引代码目录」填入 `code_root` 绝对路径 → 开始索引。
3. 索引时 DocMind 会读入 `DOCMIND_RULES.md` 存入运行时，并在每次问答时**作为第二条 system 消息**注入 Agent（优先级高于通用 `SYSTEM_PROMPT`）。
4. 换项目 = 换一个 `code_root`，规则随项目自动切换；无该文件则回退到通用指引，不报错。

> 说明：规则注入是「约束 AI 行为」，不是「保证 AI 一定听话」——它大幅降低堆叠/幻觉概率，但真正兜底还需要 **git 版本管理 + 改前确认**（见交接文档 T5 决策，建议作为下一步安全底座补齐）。
